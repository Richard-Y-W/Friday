[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "friday-research"
version = "0.1.0"
description = "Safe scholarly scanning and reporting CLI for Friday Research."
requires-python = ">=3.9"

[project.scripts]
friday = "friday.cli:main"
